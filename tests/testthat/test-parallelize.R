################################################################################

context("PARALLELIZE")

set.seed(SEED)

################################################################################

test_that("need more tests", {
  expect_length(big_parallelize(FBM(5, 1), function(X, ind) 1,
                                ncores = test_cores()), 1)
})

################################################################################
